# this code will check if an input value is greater than the second number and return the value as an integer

num1 = int(input("Enter the first number: "))
num2 = float(input("Enter the second number:"))

if num1 > num2:
    print(f"{num1} is greater than {int(num2)}")
elif num2 > num1:
    print(f"{int(num2)} is greater than {num1}")
else:
    print("Both numbers are equal")
